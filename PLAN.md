> 코드 기준: `main`, 커밋 `70ebece` (죽은 코드 정리 이후). 아래 각 항목에 원래 내용은 그대로 두고,
> 현재 코드와 대조해서 확인한 사실 / 애매한 부분 / 의존 관계를 "검토" 항목으로 덧붙였습니다.
>
> **1번, 2번 항목은 구현 완료되어 `NEXT_PLAN.md`를 정리(삭제)했습니다.** 번호는 원래 목록과 헷갈리지 않도록
> 3번부터 그대로 유지합니다. 구현된 내용 요약:
> - **1번(AI공감 자동화)**: `AIHelper`가 `summarize_diary()`(요약 전용 호출)와 `analyze_empathy()`(공감+그림분석 호출)로 분리됨. CSV에 `emotion_tier`, `summary` 컬럼 추가. 저장 버튼 클릭 시 백그라운드 스레드(`SaveWorker`/Tk의 `threading.Thread`)에서 저장→요약 대기→CSV 기록→공감분석 순으로 진행되며, 같은 모달 다이얼로그 안에서 `일기 저장 중...` → `AI가 조언 중...` → 결과 3종 표시로 상태가 전환됨. 1줄 요약은 다이얼로그 내에서 수정 후 "확인"으로 커밋 가능.
> - **2번 중 지금 구현된 부분**: `password` CSV 컬럼 제거, 전역 비밀번호를 `data/password.txt`에 평문 저장(`SecretPasswordStore`, `.gitignore` 등록됨), 숨겨진 일기는 `DiaryService.get_all_diaries`/`get_diaries_by_date_range`에서 완전히 제외되어 목록·키워드 분석 어디에도 노출되지 않음.
> - **2번 중 아직 안 만든 부분(찢기 연출, 비밀 일기장 전용 조회 UI)은 4번과 함께 `NEXT_PLAN.md`로 옮겼습니다.**
>
> **4번 항목도 아직 구현 전이라 `NEXT_PLAN.md`로 옮겼습니다.** 아래 5~7번에서 4번을 참조하던 부분은
> `NEXT_PLAN.md`를 가리키도록 정리해뒀습니다.

## 3. "위치" 데이터를 "날씨", "감정" 미리 리스트를 만들어두고 그 중에서 선택할 수 있도록 설정하기
학교, 직장, 집과 같이 미리 제공된 데이터셋이 있음.
없는 데이터 셋 ex(롯데월드)를 입력한다면 미리 제공된 데이터셋에 사용자가 입력한(롯데월드)를 추가함

**검토**
- 지금 위치는 `locationLineEdit`(자유 입력 `QLineEdit`) 하나뿐이고, 날씨/감정처럼 `domain/model/value_objects.py`에 정의된 프리셋 리스트가 없음. 날씨(`MANUAL_WEATHER_OPTIONS`)·감정(`MANUAL_EMOTION_OPTIONS`)과 동일한 구조로 `MANUAL_LOCATION_OPTIONS` 같은 상수를 추가하는 건 어렵지 않음.
- 다만 "사용자가 입력하면 프리셋에 추가"는 **영구 저장이 필요한 사용자별 데이터**라서, 코드에 박아넣는 상수(`value_objects.py`)만으로는 부족함 — 앱을 재시작해도 "롯데월드"가 남아있어야 한다면 별도 저장소(예: `data/locations.json` 같은 작은 설정 파일, 혹은 CSV에 이미 등장한 `location_name` 값들을 매번 스캔해서 프리셋에 합치는 방식)가 필요. 어떤 저장 방식을 쓸지 결정 필요.
- 콤보박스(선택) + 자유 입력(신규 추가)을 동시에 지원하려면 PyQt의 `QComboBox(editable=True)` 같은 위젯으로 바꾸는 게 자연스러움 — 날씨/감정 콤보박스와 UI 패턴이 달라짐(그쪽은 편집 불가 고정 리스트).

## 5. 필터링하는 기능의 함수 만들기.
작성한 일기들을 필터링하는 여러 곳에서 사용할것이기 때문에 일기를 필터링하는 기능을 제작하기
사용자의 위치, 날씨, 감정, 제목, 일기에서 사용된 단어, 1줄 요약에서 사용된 단어, 일기를 작성한 기간을 기반으로 필터링하는 기능을 강화.

**검토**
- 지금 필터링은 `Diary.matches_filter(filter_value: str)` 하나뿐이고, **단일 값**(날씨 이모지 1개 또는 "긍정/중립/부정 일기" 중 하나)만 받는 구조. 여기에 위치, 제목, 본문 키워드, 요약 키워드, 기간까지 **여러 조건을 동시에(AND 조합으로) 적용**하려면 함수 시그니처를 문자열 하나가 아니라 필터 조건 객체/딕셔너리(예: `location=`, `weather=`, `emotion=`, `title_contains=`, `content_keyword=`, `summary_keyword=`, `date_from=`, `date_to=`)로 바꾸는 설계 변경이 필요함. 기존 `matches_filter(filter_value)` 호출부(`DiaryService.get_all_diaries`, `app_gui.py`/`app_tkinter.py`의 `filterComboBox`)를 전부 이 새 시그니처에 맞게 고쳐야 함.
- "일기에서 사용된 단어" 필터는 `engine/text_processor.py`의 전처리(`TextProcessor.process`) 결과를 재사용하면 자연스러움 — 이미 키워드 분석에서 쓰는 파이프라인과 동일.
- "1줄 요약에서 사용된 단어" 필터는 이제 `summary` 컬럼이 CSV에 저장되고 있으니(1번 항목 구현 완료) 바로 만들 수 있음.
- 날짜 기간 필터는 이미 `CSVDiaryRepository.find_by_date_range`/`DiaryService.get_diaries_by_date_range`로 존재하니, 새 필터 함수 안에 통합하거나 나란히 두고 조합하면 됨.

## 6. 필터링함수 적용하기.
기존에 필터링을 하던 "일기 목록 검색"
"키워드 분석"에서 필터링함수를 통해서 진행할 수 있도록 하기

**검토**
- **사실 확인**: 지금 앱에는 "일기 목록 검색"이라 부를 만한 **자유 텍스트 검색창이 없음**. 왼쪽 목록 위에 있는 건 `filterComboBox` 하나뿐이고, 날씨 이모지/긍정·중립·부정 중 하나를 고르는 드롭다운이다(`app_gui.py`의 `filterComboBox`, `app_tkinter.py`도 동일 구조). 5번에서 만들 새 필터 함수를 여기 적용하려면, 위치/제목/키워드 조건을 입력받을 **새 UI 요소(검색창, 위치 선택 등)를 왼쪽 패널에 추가**하는 작업이 먼저 필요함 — "기존 검색 기능 교체"가 아니라 "새 검색 UI 신설 + 필터 함수 연결"에 더 가까움.
- "키워드 분석"(`show_mindmap_window`/`_run_keyword_analysis`, `ui/keyword_dialog.ui`) 쪽도 현재는 **기간(시작일~종료일) 조건만** 있고 날씨/감정/위치 조건은 없음. 필터 함수를 여기 적용하려면 `keyword_dialog.ui`에도 필터 입력 위젯을 추가해야 함.
- 즉 6번은 "기존 기능을 새 함수로 갈아끼우는" 리팩터링이 아니라, 두 화면(목록/키워드 분석) 모두에 **새 필터 UI를 추가하면서 5번 함수로 로직을 통일**하는 작업에 가까움 — 작업량을 그렇게 예상하고 잡는 게 좋을 듯.

## 7. UI를 대폭 수정하기
7-1 오른쪽 패널과 하단 버튼을 통합하여 MAIN페이지를 제작한다.
```
<widget class="QFrame" name="rightPanel">
<layout class="QHBoxLayout" name="buttonLayout">
```
이후 일기 목록에서 일기를 선택한다면, 선택한 페이지가 등장하도록 수정한다.

**검토**
- `rightPanel`, `buttonLayout` 위젯명 확인됨(`ui/main_window.ui:206`, `:300`). 지금 `buttonLayout`은 `rightPanel` **안에** 이미 포함된 하단 버튼 줄(저장/삭제/키워드 분석/새 일기)이라, "오른쪽 패널과 하단 버튼을 통합"이 정확히 뭘 바꾸겠다는 건지(이미 한 프레임 안에 있음) — 레이아웃 구조 변경인지, 아니면 이 버튼들을 다른 위치(예: 목록 선택 시 나타나는 페이지 쪽)로 옮기겠다는 뜻인지 확인이 필요함.
- "선택한 페이지가 등장하도록 수정" — 지금은 목록에서 일기를 클릭하면 **같은 입력 폼**(`_on_diary_selected`)에 내용을 채워 넣어 바로 수정 가능한 상태로 보여준다(별도의 "보기 전용 페이지"가 없음). 이 항목이 원하는 게 (a) 지금처럼 같은 폼에 로드하되 레이아웃만 재배치하는 것인지, 아니면 (b) 목록 선택 시 "보기 전용 상세 페이지"로 전환되고 수정하려면 별도 동작이 필요한 **모드 전환 UI**(예: `QStackedWidget`으로 "목록/작성 페이지" ↔ "상세 보기 페이지" 전환)를 새로 만드는 것인지에 따라 작업 난이도가 크게 달라짐. 후자라면 지금까지 없던 새로운 화면 상태(뷰 모드 vs 편집 모드)를 설계해야 함.
- **`NEXT_PLAN.md`(비밀 일기장 UI)와 맞물림**: 비밀 일기장 화면이 메인 화면과 완전히 분리된 별도 창/다이얼로그일지, 아니면 여기서 만들 페이지 전환 구조(예: `QStackedWidget`) 안의 한 페이지로 들어갈지는 7번 설계 시 같이 결정하는 게 좋음.
- 7번은 사실상 이번 목록에서 가장 손이 많이 가는 항목이라, 다른 항목(`NEXT_PLAN.md`·5, 6번이 만드는 새 위젯들 — "일기 찢기" 체크박스·비밀일기장 버튼, 필터 UI 등)을 다 반영한 뒤 **마지막에** 레이아웃을 재배치하는 순서가 안전해 보임. 반대로 레이아웃부터 확정하고 그 위에 기능을 얹는 순서를 원한다면, 다른 항목에서 새로 추가되는 위젯들이 어디에 배치될지를 7번 설계 단계에서 미리 자리 잡아둬야 함. (요약 표시는 이미 목록에 반영되어 있음 — 1번 항목 구현 완료.)

## 8. 그래프 그리기
감정의 변화를 표현

**검토**
- `EmotionScore.value`(숫자 점수, `domain/model/value_objects.py`의 `EMOTION_LABEL_TO_SCORE`로 -5~5 범위)가 이미 각 일기에 존재하므로, 날짜별로 이 값을 시계열로 이어 그리는 형태(꺾은선 그래프)가 가장 자연스러움. 데이터 자체는 추가 작업 없이 바로 쓸 수 있음.
- 렌더링은 `engine/keyword_analyzer.py`에서 이미 쓰고 있는 `matplotlib.figure.Figure` + `FigureCanvasAgg` 패턴을 그대로 재사용 가능(워드클라우드처럼 이미지를 만들어 Qt/Tk 쪽에 붙이는 방식). `requirements.txt`에 `matplotlib`이 이미 포함돼 있어 새 의존성 추가 불필요.
- 기간 선택(시작일~종료일)은 "키워드 분석" 다이얼로그(`ui/keyword_dialog.ui`)에 이미 있는 UI를 그대로 본뜨거나, 같은 다이얼로그에 탭/영역을 추가해 그래프를 얹는 방식이 재사용성이 높음.
- 하루에 감정을 2개(`emotion1`, `emotion2`)까지 기록할 수 있는 구조라, 그래프에 점을 하나만 찍을지(예: 평균/합산) 두 값을 별도 계열로 그릴지 결정 필요.
- `is_hidden` 일기를 그래프에 포함할지 여부도 결정 필요 — 지금 목록/키워드 분석에서는 완전히 제외하는 정책이므로, 일관성을 위해 그래프도 `DiaryService.get_all_diaries`/`get_diaries_by_date_range`가 반환하는(이미 숨김 제외된) 목록을 그대로 쓰면 됨.

## 9. 해시태그
(원문 그대로 — 상세 요구사항 없음)

**검토**
- 지금 `Diary`/CSV 스키마(`infrastructure/persistence/csv_diary_repository.py`)에는 태그를 담을 컬럼이 없음. 새 컬럼(예: `tags`, 콤마 구분 문자열)을 추가하고 `_row_to_entity`/`_entity_to_row`, 레거시 CSV 정규화 로직(누락 컬럼 기본값 처리)까지 함께 손봐야 함.
- 입력 방식 결정 필요: (a) 사용자가 직접 `#태그` 형식으로 자유 입력, (b) `engine/keyword_analyzer.py`/`TextProcessor`가 이미 추출하는 키워드를 해시태그 후보로 자동 제안, (c) 둘의 혼합(자동 제안 + 수동 추가/삭제). 이미 존재하는 형태소 분석 파이프라인을 재사용하면 (b)/(c) 쪽이 작업량이 적음.
- 저장 후 활용처를 같이 정해야 실익이 있음 — 예: 목록에 태그 칩 표시, 5번(필터링 함수)에 `tag=` 조건 추가, 태그 클릭 시 해당 태그가 달린 일기만 필터링. 즉 9번은 5·6번(필터링)과 맞물려 설계하는 게 자연스러움.

## 10. 일기장 안에서의 이점 제공
일기를 쓰면 뭔가 이점을 준다.

**검토**
- 원문이 추상적이라 먼저 "이점"의 형태를 정하는 게 우선임 — 예: (a) 연속 작성일수(스트릭) 표시, (b) 작성 횟수에 따른 배지/칭호, (c) 8번 그래프·9번 해시태그와 연동한 통계/리포트 열람 권한, (d) 그림판 브러시나 테마 같은 꾸미기 요소 잠금 해제 등. 방향에 따라 필요한 데이터가 완전히 달라짐.
- 스트릭/횟수 기반이라면 CSV에 이미 있는 `date`/`created_at`만으로 계산 가능해 새 저장소 없이 시작할 수 있음(단, 연속 작성 판정 시 `is_hidden` 일기 포함 여부 결정 필요).
- 배지·잠금해제 같은 영속적 상태가 필요하면 `SecretPasswordStore`처럼 작은 전용 저장 파일(예: `data/achievements.json`)이 하나 더 필요함 — 3번(위치 프리셋 영구 저장) 항목과 마찬가지로 "이 프로젝트에 CSV 외의 영속 저장소를 얼마나 늘릴 것인가"라는 공통 질문과 맞닿아 있음.
- 이 항목은 다른 9개 항목과 달리 순수 신규 기능(기존 UI/데이터 흐름 리팩터링이 아님)이라 우선순위·범위를 가장 먼저 명확히 해야 작업 규모를 가늠할 수 있음.

---

## 진행 순서 제안 (참고용)

의존 관계만 보면 아래 순서가 자연스러워 보입니다. 실제 순서는 우선순위에 따라 조정 가능합니다.

1. ~~1번 → 5번, 2번(일부)~~ — 1번, 2번(비밀번호 저장/목록 제외)은 구현 완료.
2. **`NEXT_PLAN.md`**: 찢기 연출 + 비밀 일기장 UI 제작 (원래 4번).
3. **3번 → 5번 → 6번**: 위치 프리셋 구조가 정해져야 필터 함수에 "위치" 조건을 자연스럽게 넣을 수 있고, 필터 함수가 있어야 6번에서 화면에 적용할 수 있음.
4. **7번**: 위 항목들이 만들어내는 위젯(찢기 체크박스/비밀일기장 버튼, 필터 UI)이 어디에 자리 잡을지 정해진 뒤 레이아웃을 재배치하는 게 재작업을 줄이는 길.

## 확인이 필요한 질문 모음

- (3번) 사용자가 추가한 위치 프리셋을 어디에(어떤 파일 형식으로) 영구 저장할지?
- (7번) 목록에서 일기를 선택했을 때 "보기 전용 페이지"가 별도로 생기는 것인지, 지금처럼 같은 편집 폼을 재사용하는지?
- (7번) 비밀 일기장 화면을 별도 창으로 만들지, 7번의 페이지 전환 구조 안에 넣을지?
- (8번) 감정 그래프에 `emotion1`/`emotion2` 두 값을 어떻게 반영할지(평균/둘 다 표시), `is_hidden` 일기 포함 여부?
- (9번) 해시태그를 사용자가 직접 입력할지, 키워드 분석 결과에서 자동 제안할지, 필터링(5·6번)과 어떻게 연결할지?
- (10번) "이점"이 구체적으로 무엇인지(스트릭/배지/잠금해제/통계 열람 등) — 방향에 따라 필요한 저장소·작업량이 크게 달라짐.

> 4번에 달려있던 찢기 연출/비밀 일기장 UI 관련 확인 사항은 `NEXT_PLAN.md`로 옮겼습니다.
